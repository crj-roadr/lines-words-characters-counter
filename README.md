# lines-words-characters-counter
A program in C that counts the number of lines, words and characters entered by user in the console.

## How to run the program
1. Ensure that `GNU Make` is installed (if not, install it first).
2. Run the command snippet below.
    ```sh
    make run
    ```
